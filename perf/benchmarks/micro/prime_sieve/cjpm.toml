[package]
  cjc-version = "1.0.5"
  name = "prime_sieve"
  version = "1.0.0"
  output-type = "executable"

# O2 optimization for benchmark fairness vs `g++ -O2`.
[profile.build]
  compile-option = "-O2"
